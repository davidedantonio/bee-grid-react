import React from 'react';
import Grid from '@webeetle/Grid';

/**
 * Containers are the basic layout element in Beegrid. You can choose between two types of container. Default container
 */
export default function GridContainer() {
  return (
    <Grid
      className="grid-container-example"
      />
  );
}
