import React from 'react';

export default function HorizontalAlignment() {
  return (
    <div>
      <h2>Horizontal Alignments</h2>
      <p>
        You can use alignment utility classes to align your <code>GridItem</code> component.
        You can specify an horizontal alignment with .justify-self-* classes on an grid item.
      </p>
    </div>
  );
}
