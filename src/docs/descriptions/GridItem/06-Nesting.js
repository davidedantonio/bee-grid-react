import React from 'react';

export default function Nesting() {
  return (
    <div className="mt-2">
      <h2>Nesting</h2>
      <p>
        To nest your content you can specify a <code>Grid</code> component,
        with a specific numbers of cols and, in a specific <code>GridItem</code>, you can declare another <code>Grid</code> component.
      </p>
    </div>
  );
}
