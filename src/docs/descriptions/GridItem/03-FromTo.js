import React from 'react';
export default function FromTo() {
  return (
    <div>
      <h2>Rows and columns</h2>
      <p>
        You can define the number of rows and columns that a grid item must occupy at certain device size.
        For example, if we take a look at previous example, we can decide that the first <code>GridItem</code> can occupy two
        columns and two rows on medium screen size.
      </p>
    </div>
  );
}
